<form class="form-horizontal" id="comment_form" {{action 'createComment' on='submit'}}>

    <div class="input-group">
      {{input value=comment.text id=(concat "comment_field_" task.id) placeholder="Type comment..." class="form-control"}}
        <div class="input-group-btn">
            <button class="btn btn-primary" type="submit"><i class="glyphicon glyphicon-plus"></i></button>
        </div>
    </div>

</form>